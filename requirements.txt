# Requirements for Adaptive FL with PPO-based Client and Quantization Selection
# Python 3.10.x | Ubuntu 22.04 | CPU-only VM (3.8 GB RAM)
# PyTorch-only stack — TensorFlow NOT included (RAM constraint)
# Pinned for reproducibility (Stage 0 SPEC.md Section 11)

# ── Federated Learning ────────────────────────────────────────────────────────
flwr==1.7.0

# ── Deep Learning (CPU-only PyTorch to save RAM) ─────────────────────────────
# Install PyTorch CPU wheels explicitly; torchvision must match torch version.
# Note: torch+cpu wheel is ~200 MB vs. ~2 GB for CUDA. Critical for this VM.
torch==2.2.2+cpu
torchvision==0.17.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Reinforcement Learning ────────────────────────────────────────────────────
stable-baselines3==2.2.1
gymnasium==0.29.1
# shimmy provides gymnasium compatibility shims for SB3
shimmy==1.3.0

# ── Numerical / Data ──────────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.0
scipy==1.12.0

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib==3.8.2
seaborn==0.13.2

# ── System Monitoring ─────────────────────────────────────────────────────────
psutil==5.9.8
prometheus-client==0.19.0

# ── Configuration & Utilities ─────────────────────────────────────────────────
pyyaml==6.0.1
tqdm==4.66.1
click==8.1.7

# ── Logging & Serialization ───────────────────────────────────────────────────
tensorboard==2.14.0    # lightweight — SB3 uses it for PPO training logs

# ── Type checking support (dev) ───────────────────────────────────────────────
# (not required at runtime; kept for IDE support)
# types-PyYAML==6.0.12
# types-tqdm==4.66.0
